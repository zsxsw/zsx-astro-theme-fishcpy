---
import { Icon } from "astro-icon/components";
import { profileConfig } from "../../config";
import {
	getCategoryList,
	getSortedPosts,
	getTagList,
} from "../../utils/content-utils";
import { url } from "../../utils/url-utils";
import ImageWrapper from "../misc/ImageWrapper.astro";

const config = profileConfig;

// 获取统计数据
const posts = await getSortedPosts();
const totalPosts = posts.length;

const tagList = await getTagList();
const totalTags = tagList.length;

const categoryList = await getCategoryList();
const totalCategories = categoryList.length;
---
<div class="card-base card-glass p-3">
    <a aria-label="Go to About Page" href={url('/about/')}
       class="group block relative mx-auto mt-1 lg:mx-0 lg:mt-0 mb-3
       w-48 md:w-full overflow-hidden rounded-xl active:scale-95">
        <div class="absolute transition pointer-events-none group-hover:bg-black/30 group-active:bg-black/50
        w-full h-full z-50 flex items-center justify-center">
            <Icon name="fa6-regular:address-card"
                  class="transition opacity-0 scale-90 group-hover:scale-100 group-hover:opacity-100 text-white text-5xl">
            </Icon>
        </div>
        <ImageWrapper src={config.avatar || ""} alt="Profile Image of the Author" class="mx-auto lg:w-full h-full lg:mt-0 "></ImageWrapper>
    </a>
    <div class="px-2">
        <div class="font-bold text-xl text-center mb-1 dark:text-neutral-50 transition">{config.name}</div>
        <div class="h-1 w-5 bg-[var(--primary)] mx-auto rounded-full mb-2 transition"></div>
        <div class="text-center text-neutral-600 dark:text-neutral-400 mb-2.5 transition">{config.bio}</div>
        <div class="flex gap-2 justify-center mb-3">
            {config.links.length > 1 && config.links.map(item =>
                    <a rel="me" aria-label={item.name} href={item.url} target="_blank" class="btn-regular rounded-lg h-10 w-10 active:scale-90">
                        <Icon name={item.icon} class="text-[1.5rem]"></Icon>
                    </a>
            )}
            {config.links.length == 1 && <a rel="me" aria-label={config.links[0].name} href={config.links[0].url} target="_blank"
                                            class="btn-regular rounded-lg h-10 gap-2 px-3 font-bold active:scale-95">
                <Icon name={config.links[0].icon} class="text-[1.5rem]"></Icon>
                {config.links[0].name}
            </a>}
        </div>
        
        <!-- 统计信息 -->
        {config.showStats && (
            <div class="grid gap-2 text-center text-sm" 
                 class:list={[
                     (config.statsConfig?.showPosts ? 1 : 0) + 
                     (config.statsConfig?.showTags ? 1 : 0) + 
                     (config.statsConfig?.showCategories ? 1 : 0) === 3 ? 'grid-cols-3' :
                     (config.statsConfig?.showPosts ? 1 : 0) + 
                     (config.statsConfig?.showTags ? 1 : 0) + 
                     (config.statsConfig?.showCategories ? 1 : 0) === 2 ? 'grid-cols-2' : 'grid-cols-1'
                 ]}>
                {config.statsConfig?.showPosts && (
                    <div class="p-2 transition">
                        <div class="font-bold text-lg text-[var(--primary)]">{totalPosts}</div>
                        <div class="text-neutral-500 dark:text-neutral-400">文章</div>
                    </div>
                )}
                {config.statsConfig?.showTags && (
                    <div class="p-2 transition">
                        <div class="font-bold text-lg text-[var(--primary)]">{totalTags}</div>
                        <div class="text-neutral-500 dark:text-neutral-400">标签</div>
                    </div>
                )}
                {config.statsConfig?.showCategories && (
                    <div class="p-2 transition">
                        <div class="font-bold text-lg text-[var(--primary)]">{totalCategories}</div>
                        <div class="text-neutral-500 dark:text-neutral-400">分类</div>
                    </div>
                )}
            </div>
        )}
    </div>
</div>